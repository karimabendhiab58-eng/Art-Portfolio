const CONFIG = {
    // URL de ton API Laravel (vérifie que ton serveur tourne sur le port 8000)
    API_URL: "http://127.0.0.1:8000/api",
    
    // URL vers le dossier des images (le lien symbolique + ton sous-dossier)
    STORAGE_URL: "http://127.0.0.1:8000/storage/oeuvres/"
};